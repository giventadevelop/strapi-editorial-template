---
description: Production MOSC news — tenant on publish, category slugs, naming standards, Cloud image/cover repair, and push gotchas
globs: src/bootstrap.js,src/utils/tenant-assignment.js,src/api/article/**/*,src/api/category/**/*,src/index.js,scripts/debug-production-news.mjs,scripts/repair-production-article-tenant*.mjs,scripts/normalize-production-category-slugs.mjs,scripts/repair-cloud-news-sync*,scripts/sync-article-published-dates*,scripts/wipe-cloud-articles*,scripts/seed-ads-flash*,scripts/repair-cloud-ad-s3*
alwaysApply: false
---

# Strapi Cloud production news — tenant, categories, slugs, and cover repair

## Problem (symptoms)

- Production news page shows **“No articles”** while local works.
- **Tenant filter** returns 0 when published rows lack `articles_tenant_lnk` (draft may have tenant; published does not).
- **Category sections empty** when production slugs are `Main-News` but frontend filters `main-news`.
- Covers/ads show placeholders after Cloud push or when S3 keys point at the wrong prefix.

## Root causes

1. **Strapi 5 Draft & Publish**: draft and published are separate DB rows; **relation link tables are not always copied on publish**.
2. **Category slug casing**: UID fields stored with wrong casing break `filters[category][slug][$eq]=main-news`.
3. **Media stripped / wrong S3 prefix** on tenant push: covers need durable S3 register, then tenant re-link (see gotchas below).

## Enforcement (code — requires Strapi Cloud deploy)

| Layer | File | Behavior |
|-------|------|----------|
| Article lifecycles | [`src/api/article/content-types/article/lifecycles.js`](mdc:src/api/article/content-types/article/lifecycles.js) | `beforeCreate` / `beforeUpdate`: assign tenant from editor-tenant mapping |
| Slug lifecycles + middleware | [`src/utils/normalize-slug.js`](mdc:src/utils/normalize-slug.js), per-type `lifecycles.js`, [`src/bootstrap.js`](mdc:src/bootstrap.js) | All UID `slug` fields → lowercase kebab-case on save; optional `STRICT_KEBAB_SLUG=1` rejects invalid slugs with a validation error |
| Publish middleware | [`src/bootstrap.js`](mdc:src/bootstrap.js) `registerTenantPublishMiddleware()` | **Before** publish: ensure draft has tenant; **after** publish: copy tenant link to published row |
| Shared helpers | [`src/utils/tenant-assignment.js`](mdc:src/utils/tenant-assignment.js) | Editor→tenant resolution, link-table ensure/copy |
| Editor RBAC | [`src/bootstrap.js`](mdc:src/bootstrap.js) `registerTenantDocumentMiddleware()` | Tenant-scoped find/update for editor role |

**Tenant-scoped UIDs** (publish middleware): article, advertisement-slot, flash-news-item, directory-home, and all directory collection types listed in `registerTenantPublishMiddleware`.

## News push gotchas (articles + ads)

- **`push:tenant-to-cloud` publish resets `publishedAt` to “now”.** After pushing articles, restore historical dates from local:
  ```bash
  npm run sync:article-published-dates-cloud -- --tenant-id=mosc_malankara_orthodox_2
  ```
  Uses `POST /api/migration/fix-published` (knex) so Document Service publish does not overwrite dates again.
- **Cover PUT can drop tenant/category (and previously wiped title/slug).** After S3 article covers, re-link tenants. Prefer the orchestrator:
  ```bash
  npm run repair:cloud-news-sync -- --tenant-id=mosc_malankara_orthodox_2
  ```
  S3 linker preserves `title`/`slug`/… and maps category by **Cloud slug**; still re-run tenant fix after cover link.
- **Clean article re-upload (conflicts):** `npm run wipe:cloud-articles-tenant -- --tenant-id=mosc_malankara_orthodox_2` then push articles only + dates + S3.
- **Local ads/flash scrape:** `npm run seed:ads-flash-catholicatenews -- --tenants=tenant_demo_002,mosc_malankara_orthodox_2`
- **Durable covers/ads media:** use S3 strategy A — [strapi-cloud-production-durable-s3-image-upload-not-ephemeral-uploads.mdc](mdc:.cursor/rules/strapi-cloud-production-durable-s3-image-upload-not-ephemeral-uploads.mdc) (`--collection=articles` / `advertisement-slots`, `--skip-api`).
- **S3 register with `prod/` prefix while local media lives under `dev/`** creates Cloud URLs that **403** (object missing under prod). `push-collection-images-s3` now copies remote `dev/` bytes into the prod key before register. Emergency repair:
  ```bash
  node scripts/repair-cloud-ad-s3-from-dev.js
  ```
- Bucket has **ACLs disabled** — do not pass `ACL: public-read` on PutObject; see [strapi-aws-s3-upload-omit-acl-when-bucket-owner-enforced.mdc](mdc:.cursor/rules/strapi-aws-s3-upload-omit-acl-when-bucket-owner-enforced.mdc).
- **Full news Cloud runbook:** [news-production-cloud-push-prd.html](mdc:documentation/front_end_based_collection_types_import/production_cloud_data_push/news-production-cloud-push-prd.html)

## One-time Cloud repair (no new deploy required for endpoint)

Uses existing **`POST /api/migration/fix-published`** in [`src/index.js`](mdc:src/index.js):

```json
{
  "tenantDocumentId": "<tenant documentId from /api/tenants>",
  "articles": [{ "documentId": "...", "uid": "api::article.article" }]
}
```

Auth: `Authorization: Bearer` = `STRAPI_CLOUD_API_TOKEN` or `STRAPI_MIGRATION_TOKEN`.

### Scripts

| Script | Purpose |
|--------|---------|
| [`scripts/debug-production-news.mjs`](mdc:scripts/debug-production-news.mjs) | Read-only: tenant, articles, category slug probes |
| [`scripts/repair-production-article-tenant-published.mjs`](mdc:scripts/repair-production-article-tenant-published.mjs) | Batch `fix-published` for all article documentIds |
| [`scripts/repair-production-article-tenant.mjs`](mdc:scripts/repair-production-article-tenant.mjs) | PUT tenant on draft/published via Document API |
| [`scripts/normalize-production-slugs.mjs`](mdc:scripts/normalize-production-slugs.mjs) | Backfill all slug fields on Cloud/local (`--only=categories` optional) |
| [`scripts/normalize-production-category-slugs.mjs`](mdc:scripts/normalize-production-category-slugs.mjs) | Categories only (deprecated; use normalize-production-slugs) |
| [`scripts/fix_article_category_tenant.js`](mdc:scripts/fix_article_category_tenant.js) | Local DB: propagate category/tenant links to all article rows |

**Env**: `STRAPI_CLOUD_URL`, `STRAPI_CLOUD_API_TOKEN`, `TENANT_ID` (default `tenant_demo_002`). Always prefer explicit `--tenant-id=` for production (`mosc_malankara_orthodox_2`).

**Windows TLS** (if `fetch failed`):

```powershell
$env:NODE_TLS_REJECT_UNAUTHORIZED='0'
```

## Operational runbook (order)

1. **Diagnostics** (read-only): `node scripts/debug-production-news.mjs`
2. **Published tenant backfill** (if tenant filter = 0): `node scripts/repair-production-article-tenant-published.mjs --tenant-id=tenant_demo_002`
3. **Slug normalize** (if lowercase filters = 0): `node scripts/normalize-production-slugs.mjs` (use `--dry-run` first; `--only=categories` for categories only)
4. **Re-verify**: `node scripts/debug-production-news.mjs` — expect tenant-primary rows and category slugs `main-news`, `press-release`, `featured-news`
5. **Deploy Strapi** with lifecycle + middleware changes (see below) so **future** publishes keep tenant and slugs
6. **Frontend**: confirm production news page; hard-refresh / invalidate CDN if cached

**npm**: `npm run normalize:slugs-cloud` or `npm run normalize:category-slugs-cloud` (categories only)

## When to deploy Strapi Cloud

Deploy **after** committing/pushing these paths:

- `src/utils/tenant-assignment.js`
- `src/bootstrap.js` (tenant publish middleware)
- `src/api/article/content-types/article/lifecycles.js`
- `src/utils/normalize-slug.js`
- `src/api/category/content-types/category/lifecycles.js`
- `src/api/article/content-types/article/lifecycles.js` (and other slug types: tenant, bishop, diocese, parish, priest, directory-entry)

**Timing**:

- Steps 1–4 (repair scripts) can run **before or after** deploy; they use REST + existing migration route.
- Deploy is required so **new** publishes/updates automatically keep tenant on published rows and category slugs stay normalized in the CMS.

Do **not** wait for deploy to run slug normalization or `fix-published` backfill.

## Content API query patterns (frontend / Next)

- **Tenant filter**: prefer OR of `filters[tenant][tenantId][$eq]` and `filters[tenant][documentId][$eq]` when populate is unreliable.
- **Category filter**: use lowercase slugs: `main-news`, `press-release`, `featured-news`, `most-read`.
- **`populate=tenant`**: may still show `tenant: null` on list responses even when link table is correct; trust tenant filter counts from debug script.

## Collection type naming standards

| Field | Standard | Example |
|-------|----------|---------|
| `singularName` / `pluralName` | lowercase, kebab-case where multi-word | `article` / `articles`, `retired-bishop` / `retired-bishops` |
| `displayName` | Human UI label only (any casing, en-dash OK) | `Editorial – Article`, `Directory – Bishops` |
| REST path | `/api/{pluralName}` | `/api/articles`, `/api/categories` |
| UID | `api::{singular}.{singular}` | `api::article.article` |
| Any `slug` UID field | **lowercase kebab-case** | `main-news`, article/parish/directory slugs, etc. |

## Slug validation in Content Manager

- **While typing**: Strapi’s built-in **UID** field does **not** show live kebab-case errors (it allows `Main-News`, underscores, etc.). There is no native “validate as you type” for kebab-only on UID fields without a **custom admin field** (large change).
- **On save (default)**: `applyKebabSlugToEvent` **auto-corrects** to kebab-case (e.g. `Main-News` → `main-news`). Field **description** in schema hints editors under the slug input.
- **On save (strict)**: Set `STRICT_KEBAB_SLUG=1` in `.env` / Cloud env → save **fails** with `ValidationError` if slug is not already valid kebab-case (admin shows API error).
- **Content-Type Builder**: For a **Text** field (not UID), Advanced settings → **Regexp** can add `^[a-z0-9]+(-[a-z0-9]+)*$` — but you lose UID auto-generation from title/name.

**Do not** rename collection types in DB for API fixes. Refactor `displayName` only for editor clarity.

## Related rules

- [strapi-nextjs-frontend-slug-contract-kebab-case-category-filters.mdc](mdc:.cursor/rules/strapi-nextjs-frontend-slug-contract-kebab-case-category-filters.mdc) — **share with Next.js**: category slugs, article URLs, no frontend validation on read
- [strapi5-content-api-frontend-query-patterns-tenant-populate-published.mdc](mdc:.cursor/rules/strapi5-content-api-frontend-query-patterns-tenant-populate-published.mdc) — Content API tenant / populate / publishedAt patterns
- [strapi-cloud-bootstrap-content-manager-config-sync-and-default-sort-fixes.mdc](mdc:.cursor/rules/strapi-cloud-bootstrap-content-manager-config-sync-and-default-sort-fixes.mdc) — CM config after transfer
- [strapi-cloud-production-durable-s3-image-upload-not-ephemeral-uploads.mdc](mdc:.cursor/rules/strapi-cloud-production-durable-s3-image-upload-not-ephemeral-uploads.mdc) — durable S3 for covers/ads
- [strapi5-local-news-import-propagate-category-tenant-cover-draft-and-published.mdc](mdc:.cursor/rules/strapi5-local-news-import-propagate-category-tenant-cover-draft-and-published.mdc) — local seed D&P propagation
- Push/import: [`scripts/rest_api_push_to_cloud.js`](mdc:scripts/rest_api_push_to_cloud.js) uses `normalizeSlug()` and case-insensitive category dedup

## Checklist after changes

- [ ] `debug-production-news.mjs`: tenant-primary > 0 rows
- [ ] Category probes `main-news`, `press-release`, `featured-news` return rows
- [ ] Strapi Cloud deployed with tenant-assignment + lifecycles
- [ ] Editor mapped via `scripts/assign-editor-to-directory-tenant.js` if CM user cannot see tenant content
- [ ] Article covers / ads use S3 absolute URLs with `HEAD` 200 (not `/uploads/`)
