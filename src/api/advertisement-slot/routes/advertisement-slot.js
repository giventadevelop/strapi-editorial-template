'use strict';

/**
 * advertisement-slot router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::advertisement-slot.advertisement-slot');
