'use strict';

/**
 * advertisement-slot service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::advertisement-slot.advertisement-slot');
