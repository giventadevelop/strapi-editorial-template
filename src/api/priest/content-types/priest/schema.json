{
  "kind": "collectionType",
  "collectionName": "priests",
  "info": {
    "singularName": "priest",
    "pluralName": "priests",
    "displayName": "Directory – Priests",
    "description": "Priest: name, title, diocese, optional parish (as vicar), contact"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "name": {
      "type": "string",
      "required": true
    },
    "slug": {
      "type": "uid",
      "targetField": "name",
      "required": true
    },
    "title": {
      "type": "string",
      "description": "e.g. Vicar, Fr."
    },
    "diocese": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::diocese.diocese",
      "required": true,
      "inversedBy": "priests"
    },
    "parish": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::parish.parish",
      "mappedBy": "vicar",
      "description": "Parish where this priest is vicar (optional)"
    },
    "address": {
      "type": "text"
    },
    "email": {
      "type": "string"
    },
    "phones": {
      "type": "text",
      "description": "Phone number(s), comma-separated if multiple"
    },
    "image": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"]
    },
    "tenant": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::tenant.tenant",
      "description": "Auto-assigned from editor's tenant. Do not edit.",
      "pluginOptions": {
        "content-manager": {
          "visible": false
        }
      }
    }
  }
}
